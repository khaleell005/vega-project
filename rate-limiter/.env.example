PORT=3000
REDIS_HOST=localhost
REDIS_PORT=6379

PGHOST=127.0.0.1
PGPORT=5433
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=rate_limiter

# Docker Compose host port overrides (only needed if defaults conflict)
# POSTGRES_HOST_PORT=5433
# REDIS_HOST_PORT=6380
# APP_HOST_PORT=3000
# DASHBOARD_HOST_PORT=5173
# TEST_CLIENT_HOST_PORT=8080
